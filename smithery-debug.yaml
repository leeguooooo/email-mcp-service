name: mcp-email-service
version: 1.0.5
description: A unified MCP email service supporting multi-account management

# MCP server configuration  
mcp:
  type: stdio
  localOnly: true

# Simple test command
commandFunction: |
  (config) => ({
    command: "echo",
    args: ["MCP Email Service would start here"],
    env: {}
  })

# Minimal install for debugging
installCommand:
  command: echo
  args: ["Installation would happen here"]