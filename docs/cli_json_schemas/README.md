# CLI JSON Schemas

These JSON Schemas lock the minimum stable surface of `--json` output.

They intentionally validate only the required/core fields needed by skills.
The CLI may include additional fields (forward-compatible additions).
